name: Cassa

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Cypress tests
        uses: cypress-io/github-action@v4
        with: 
          command: npm cypress run --spec "./cypress/e2e/PaymentCheck.cy.js"
          browser: chrome
    
      
